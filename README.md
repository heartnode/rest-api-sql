# Project 9 Rest API and SQL
This project uses Node.js, Express, Sequelize, bcryptjs and basic-auth. It implements an artificial Users and Courses Rest API site.

To run:

1. npm install
2. npm run seed
3. npm start